name: cinebot
description: "A new Flutter project."
publish_to: 'none' 

#App version
version: 1.0.0+1
#
environment:
  sdk: ^3.8.1
#
##Dependencies
dependencies:
  flutter:
    sdk: flutter

  hive_ce: ^2.15.1            # Hive database community edition
  hive_ce_flutter: ^2.3.3     # Hive flutter integrator
  get: ^4.7.2              # For State Management
  path_provider: ^2.1.3    # For Accessing Storage Path
#
  flutter_dotenv: ^6.0.0
  markdown_widget: ^2.3.2+8
  cupertino_icons: ^1.0.8
  lottie: ^3.3.1  #For using lottie animations

  http: ^1.5.0            # For API Call

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.7        # The program that executes the code generators
  hive_ce_generator: ^1.10.1  # The generator specific to Hive CE, creates TypeAdapters
#
##For generating app icons
  # icons_launcher: ^3.0.2


flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
    - assets/lottie/
  #fonts:
  #      -family: Schyler
  #       fonts:
  #        - asset: fonts/Schyler-Regular.ttf
  #        - asset: fonts/Schyler-Italic.ttf
  #          style: italic
  #      - family: Trajan Pro
  #        fonts:
  #         - asset: fonts/TrajanPro.ttf
  #         - asset: fonts/TrajanPro_Bold.ttf
  #           weight: 700
